**Tasks:**

- Ran the Genetec Middleware
- Updated GWS related ticket https://calgarystampede.freshservice.com/a/tickets/1431?current_tab=details

**Notes:**

Okta Workflows has various `connectors`. These connectors help establish connections/integrations with either an external SaaS service or a service we host on our environment. These connectors help us send/push data to the connector system or retrieve/pull data from it. Connectors that are out of box like Amazon, Google, Salesforce will ask for details to create and establish the connection. For Example: 
- For Amazon S3 the connector will need an Account ID, External ID, and Role ARN (Amazon Resource Name).
- For API Connectors we will need the Authentication type and then the details for authenticating. If it is Custom then it will give the Header Name and Header Value.
We would need to use the `connection builder` for connections which do not have a native integration or template for the connection within Okta.

The `Google User Table Data Load` uses the API Endpoint card which receives data (this card helps us send data to the workflow) and the workflow will have its own Endpoint URL, and also Client Token.

Error Blocks are useful because if we are processing multiple records then we can have a try-if error condition where if the flow runs into an error and the run doesn't succeed then it won't quit processing the records entirely but move on to the next record. This helps us **ignore** the error.

We could have all the information from SF in Okta Staging Tables and then have the create/update flows retrieve data from these tables to create/update the necessary scenarios which could be Account create/update or Event create/update etc. 